import { Playball, Inconsolata } from "next/font/google";

export const playball = Playball({
  subsets: ["latin"],
  display: "swap",
  weight: ["400"],
});
export const inconsolata = Inconsolata({ subsets: ["latin"], weight: ["500"] });
