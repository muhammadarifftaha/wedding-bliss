import React, { useState } from "react";

export default function AdminNav() {
  const [isExpanded, setIsExpanded] = useState(false);
  const [isDocked, setIsDocked] = useState(false);
  return <aside>AdminNav</aside>;
}
